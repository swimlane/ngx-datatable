import { Section, SectionProp } from '../types';
export declare function sectionRows(rows: any[], prop: SectionProp, sections: Section[]): {
    rows: any;
    rowSections: Map<any, any>;
    sectionCounts: any[];
};
